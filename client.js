$(document).ready(onReady);

function onReady() {
    console.log(" This is going to work!")
}
const employees =[
    {
        firstName,
        lastName,
        iD,
        title,
        annualSalary,

    },
];

function employeeAdd(employees) {
    this.firstName = employee.firstName;
    this.lastName = employee.lastName;
    this.id = employee.id;
    this.title = employee.title;
    this.annualSalary = employee.annualSalary;
    return employee
}